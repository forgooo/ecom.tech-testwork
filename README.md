# Student Grades API

FastAPI приложение для загрузки и анализа оценок студентов.

---

## 🚀 Запуск

```powershell
# 1. Пересобрать контейнеры
docker compose up -d --build

# 2. Если БД не создалась, подождать 10 сек и выполнить
docker compose up -d

# 3. Применить миграции БД
alembic upgrade head

# 4. Открыть документацию
http://localhost:8000/docs
```

**Готово!** Приложение доступно на `localhost:8000`

---

## 📝 Работа с проектом

### Остановка контейнеров

```powershell
docker compose down
```

### Полная очистка (удалит данные БД)

```powershell
docker compose down -v
```

### Просмотр логов

```powershell
docker compose logs app -f
docker compose logs db -f
```

---

## 📤 Загрузка оценок

**Формат CSV:** Дата;Номер группы;ФИО;Оценка

```powershell
curl -X POST http://localhost:8000/api/upload-grades `
  -F "file=@data.csv"
```

---

## 🔌 API Endpoints

- **POST** `/api/upload-grades` — загрузить CSV с оценками
- **GET** `/api/students/more-than-3-twos` — студенты с 3+ двойками
- **GET** `/api/students/less-than-5-twos` — студенты с <5 двойками

Полная документация: `http://localhost:8000/docs`

---

## 🗄️ Миграции БД

### Просмотр истории

```powershell
alembic history
```

### Создание новой миграции

```powershell
alembic revision -m "Описание изменения"
```

### Применение миграций

```powershell
alembic upgrade head      # Применить все
alembic downgrade -1      # Откатить последнюю
```

---

## 🧪 Тесты

```powershell
# Запуск всех тестов
pytest tests/ -v

# Запуск конкретного теста
pytest tests/test_upload.py -v

# С покрытием кода
pytest tests/ --cov=app
```

---
**Версия:** 1.0.0 | **Дата:** 26 декабря 2025
